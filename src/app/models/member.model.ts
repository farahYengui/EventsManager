// Définition d'une interface pour représenter un membre
export interface Member {
  Nom: string; // Propriété pour stocker le nom du membre (de type chaîne de caractères)
  Age: number; // Propriété pour stocker l'âge du membre (de type nombre entier)
}
